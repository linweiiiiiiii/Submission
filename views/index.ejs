<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Food review Webpage</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <style>
        .card-img-top {
            height: 150px;
            object-fit: contain;
            width: auto;
        }
    </style>
</head>
<body>
    <%- include('partials/navbar.ejs') %>
    <br>

    <div>
        <h1>Welcome to this Food Review page</h1>
        <p>We offer a wide variety of food establishments which can help u satisfy your every craving.</p>
    </div>
    
    <div class="container">
        <br>
        <div class="text-center"><h2> Establishments from establishments app Database</h2></div>
        <br>
      
        <table class="table table-hover small text-center">
          <thead>
              <tr>
                <th width="50">Establishment Name</th>
                <th width="1000">Image</th>
                <th width="1000">Description</th>
                <th width="100">Rating ( / 5)</th>
                <th width="50">Dining type</th>
                <th width="100">Address</th>
                <th width="50">Opening Hours</th>
                <th width="100">Menu</th>
              </tr>
          </thead>
          <tbody>
            <% for(let i=0; i < establishments.length; i++) { %>
              <tr>
                <td><a href="/establishment/ <%= establishments[i].establishment_id %>" ><%= establishments[i].place_name %> </a></td>
                <td><img src = "images/<%= establishments[i].image %>" width="100%"></td>
                <td><%= establishments[i].short_description %></td>
                <td><%= establishments[i].rating %></td>
                <td><%= establishments[i].dining_type %></td>
                <td><%= establishments[i].address %></td>
                <td><%= establishments[i].opening_hours %></td>
                <td> <%= establishments[i].menu %></td>

                <!--Edit Link-->
                <td><a href="/editestablishment/<%= establishments[i].establishmentId %>"> Edit </a></td>
                <!--Delete Link-->
                <td><a href="/deleteEstablishment/<%= establishments[i].establishment_id %>" onclick="return confirm('Are you sure you want to delete this establishment?')"> Delete </a></td>
              </tr>
            <% } %>
          </tbody>
        </table>
      </div>

</body>
</html>